name: "Web-Beest - Paragraphs"
description: "Paragraph types" 
type: "Admin"
install:
  - ckeditor5
  - editor
  - filter
  - media
  - options
  - paragraphs
  - video_embed_field
  - video_embed_media
  - viewsreference
  - webform
config:
  import:
    ckeditor5: '*'
    editor: '*'
    filter: '*'
    media: '*'
    paragraphs: '*'
    video_embed_field: '*'
    video_embed_media: '*'
    viewsreference: '*'
    webform: '*'
  strict: false
  actions:
    user.role.authenticated:
      grantPermissions:
        - 'use text format webform_default'
        - 'view media'
    user.role.administrator:
      createIfNotExists:
        label: 'Administrator'
      grantPermissions:
        - 'use text format full_html'
    user.role.editor:
      createIfNotExists:
        label: 'Editor'
      grantPermissions:
        - 'use text format full_html'

